---
interface Props {
  href?: string;
  text: string;
  class?: string;
}

const { href = '#', text, class: className = '' } = Astro.props;
---

<a
  href={href}
  class={`inline-block bg-dark text-gold-cream font-heading uppercase tracking-wide text-sm px-8 py-4 no-underline transition-colors duration-300 hover:bg-gold hover:text-dark ${className}`}
>
  {text}
</a>
